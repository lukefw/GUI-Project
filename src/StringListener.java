
public interface StringListener {
	//wen you listen to an event, have an action listener, which has action performed method
	//can make it fire an event to a listener that implements this when someone clicks a button 
	public void textEmitted(String text);
	
}
